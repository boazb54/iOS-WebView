import SwiftUI

struct ContentView: View {
    var body: some View {
        WebView(url: URL(string: "https://vibelist-plus-git-test-boazs-projects-89c69ca6.vercel.app/")!)
            .ignoresSafeArea()
    }
}
